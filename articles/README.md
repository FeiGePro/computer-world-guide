# Articles

这里存放面向阅读的科普文章。一篇文章可以横跨多个概念，但只选择一个 `primary_domain` 作为物理存放位置。

目录编号与总体架构中的 20 个一级领域一致。

文件名使用稳定的英文 kebab-case ID，例如：

```text
articles/01-foundations/what-counts-as-a-computer.md
```

文章不承担概念的唯一权威定义；稳定定义保存在 `concepts/`。
